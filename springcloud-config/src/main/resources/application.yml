spring:
  application:
    name: springcloud-config
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/outman123/springcloud/tree/master/config.git
          search-paths: respo
          username: outman123
          password: zZ681476851

#一般默认8888
server:
  port: 8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8100/eureka/
